public class Compare {

    public int compare(String[] a, String[] b) {
        int side_a = 0;
        int common = 0;
        int size_b = 0;
        Arrays.sort(a);
        Arrays.sort(b);

        return 5;
    }
}